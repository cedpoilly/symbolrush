services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: symbolrush
      POSTGRES_PASSWORD: symbolrush
      POSTGRES_DB: symbol_rush
    ports:
      - '5434:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
